# In dev: leave blank — Vite's proxy forwards /api to localhost:3001.
# In production (Vercel): set this to your backend URL, e.g.
#   VITE_API_URL=https://propertyiq-backend.onrender.com
VITE_API_URL=
